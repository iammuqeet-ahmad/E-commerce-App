

<div class="container">
  <%@products.each do |product|%>
    
  <div class="card mt-4" style="width: 18rem;">
    <img class="card-img-top" src="..." alt=<%=product.name%>>
    <div class="card-body">
      <h5 class="card-title"><%=product.name%></h5>
      <p class="card-text"><%=product.description%></p>
    </div>
    <ul class="list-group list-group-flush">
      <li class="list-group-item"><%=product.price%></li>
    </ul>
    <div class="card-body">
      <%=link_to "Show Details", product_path(product), class: "btn btn-info mt-4"%>
      <%=link_to "Add to Cart", root_path, class: "btn btn-success mt-4"%>
    </div>
  </div>
  <%end%>

<%if Product.count==0%>
  <h3>No Products are avaiable</h3>
<%end%>

`    <%=link_to "Add Product", new_product_path,method: :get, class: "btn btn-primary mt-4"%>
    <div class="d-flex mt-4">
    <%= paginate @products %>
    </div>`

</div>



<%
=begin%>
 
<div class="container">
<table class="table table-striped table-bordered table-hover ">
  <tr>
    <th>Serial No.</th>
    <th>Name</th>
    <th>Description</th>  
    <th>Price</th>
  </tr>

 <%@products.each do |product|%>
  <tr>
    <td><%=product.serialNo%></td>
    <td><%=product.name%></td>
    <td><%=product.description%></td>
    <td><%=product.price%></td>
  
    



 <td><button class="btn btn-primary"><%=link_to "Edit", edit_article_path(article), class: "text-light text-decoration-none" %></button></td>
    <td><button class="btn btn-info"><%=link_to "Show Details", article_path(article), class: "text-light text-decoration-none" %></button></td>
    <td><button class="btn btn-danger"><%=link_to "Delete", article_path(article), method: :delete, class: "text-light text-decoration-none" %></button></td> 

  </tr>
  <%end%> 

</table> 
<%
=end%>

